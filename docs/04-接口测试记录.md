# 04-接口测试记录

## 阶段 19：第一版后端闭环联调

联调日期：2026-06-27  
入口地址：`http://localhost:9000`  
Nacos 地址：`localhost:18848`

## 环境

中间件：

- MySQL：`localhost:3307`，账号 `root/root`
- Redis：`localhost:6379`
- RabbitMQ：`localhost:5672`，管理页 `http://localhost:15672`，账号 `guest/guest`
- Nacos：`http://localhost:18848/nacos`，账号 `nacos/nacos`
- Elasticsearch：`http://localhost:9200`
- MinIO：API `http://localhost:9100`，Console `http://localhost:9101`，账号 `minioadmin/minioadmin`

服务：

- Gateway：`9000`
- user-service：`9010`
- product-service：`9020`
- search-service：`9030`
- coupon-service：`9040`
- order-service：`9050`
- file-service：`9060`
- cart-service：`9070`
- home-service：`9080`

## 启动验证

以下 Gateway ping 均返回 `code = 200`：

- `GET /api/user/test/ping`
- `GET /api/product/test/ping`
- `GET /api/search/test/ping`
- `GET /api/file/test/ping`
- `GET /api/coupon/test/ping`
- `GET /api/order/test/ping`
- `GET /api/cart/test/ping`
- `GET /api/home/test/ping`

## 测试顺序与结果

1. 注册普通用户：通过。
2. 注册管理员测试用户：通过，联调时将测试账号 `role` 更新为 `ADMIN` 用于发货。
3. 登录获取 token：通过。
4. 新增商品分类：通过，测试分类 ID 为 `13`。
5. 上传商品图片到 MinIO：通过，返回 URL 前缀为 `http://localhost:9100/youxuan-mall/products/`。
6. 新增商品并保存 `mainImage`：通过，测试商品 ID 为 `33`。
7. 查询商品详情并验证 Redis：通过，`youxuan:product:detail:33` 存在。
8. 导入商品到 Elasticsearch：通过，导入数量为 `31`。
9. 搜索商品：通过，关键词搜索返回结果。
10. 修改商品后验证 RabbitMQ 同步 ES：通过，等待 6 秒后可搜索到修改后的商品名。
11. 加入购物车并勾选商品：通过，购物车项 ID 为 `6`，`checked = 1`。
12. 新增收货地址并设置默认地址：通过，默认地址 ID 为 `17`。
13. 创建优惠券：通过，优惠券 ID 为 `11`。
14. 预热优惠券：通过。
15. 领取优惠券：通过，我的优惠券中可查到该优惠券。
16. 进入订单确认页并计算价格：通过，`payAmount = 356.00`。
17. 提交订单并扣减库存：通过，订单 ID 为 `16`。
18. 模拟支付：通过，订单状态变为 `1` 待发货。
19. 管理员发货：通过，订单状态变为 `2` 待收货。
20. 用户确认收货：通过，订单状态变为 `3` 已完成。
21. 创建订单后不支付，验证超时取消：通过，测试订单 ID 为 `17`，75 秒后状态为 `4` 已取消。
22. 首页 Banner 新增、修改、删除：通过，测试 Banner ID 为 `2`。
23. 首页推荐商品新增、删除：通过，测试推荐位 ID 为 `2`。
24. 首页聚合接口：通过，返回 `banners`、`categories`、`hotProducts`、`recommendProducts`。
25. 首页聚合 Redis 缓存：通过，第二次访问后 `youxuan:home:index` 存在。
26. Banner 变更后删除首页缓存：通过，删除 Banner 后 `youxuan:home:index` 不存在。

## 关键结果

```text
productId = 33
mainImage 返回正确
Redis 商品详情缓存存在
ES 导入数量 = 31
MQ 修改同步后搜索结果存在
订单支付状态 = 1
订单发货状态 = 2
订单收货状态 = 3
超时订单状态 = 4
首页聚合缓存存在 = 1
删除 Banner 后首页聚合缓存 = 0
```

## 未覆盖内容

- 未实现 Canal。
- 未实现 Sentinel。
- 未实现 Seata。
- 未实现秒杀。
- 未开始前端。
- 未修改 `docker/docker-compose.yml`。

## 阶段 21-4：前端演示功能补全

联调日期：2026-06-27  
入口地址：`http://localhost:9000`  
前端地址：`http://127.0.0.1:3000`

### 本阶段新增或补充接口

优惠券中心：

```http
GET /api/coupon/page?pageNum=1&pageSize=20
POST /api/coupon/{id}/receive
GET /api/coupon/my
```

管理员订单管理：

```http
GET /api/order/admin/page?pageNum=1&pageSize=10
GET /api/order/admin/page?pageNum=1&pageSize=10&status=1
POST /api/order/{id}/ship
```

说明：

- `GET /api/coupon/page` 已在 Gateway 放行，游客可查看优惠券列表。
- `POST /api/coupon/{id}/receive`、`GET /api/coupon/my` 仍需要登录。
- `GET /api/order/admin/page` 只允许 `ADMIN` 角色访问。
- 购物车、地址、订单、我的优惠券和后台页面仍需要 `Authorization: Bearer token`。

### 前端页面验证项

已补充页面：

- `/coupons` 优惠券中心，展示可领取优惠券并支持登录用户领取。
- `/my-coupons` 我的优惠券，展示全部、未使用、已使用、已过期。
- `/admin/orders` 后台订单管理，支持状态筛选和待发货订单发货。

图片与缓存：

- 商品卡片统一适配 `mainImage`，兼容 `main_image`、`imageUrl`、`productImage`。
- 商品主图为空或加载失败时使用前端本地占位图。
- 后台商品管理更新商品主图后，商品服务会删除商品详情、首页聚合、热门商品和推荐商品缓存。

### 演示数据结果

已执行 `scripts/prepare-demo-data.ps1`，清理并重建本地演示数据：

```text
删除：cart_item 4、mall_order_item 3、mall_order 1、user_address 2、user_coupon 0、coupon 2、home_recommend 4、home_banner 2、product_stock 9、product 9、product_category 5
新增：product_category 8、product 40、product_stock 40、home_banner 3、home_recommend 8、coupon 5、user_address 1
Redis：首页和商品详情缓存已清理
Elasticsearch：重新导入商品 40 条
```

详细记录见：

```text
docs/06-演示数据准备.md
```

### 未做内容

- 未实现 Canal。
- 未实现 Sentinel。
- 未实现 Seata。
- 未实现秒杀。
- 未修改 `docker/docker-compose.yml`。

## 阶段 21-5：真实接口响应结构复查

复查日期：2026-06-27  
入口地址：`http://localhost:9000`

### 复查接口与字段结构

`GET /api/home/index` 返回 `code=200`。真实字段结构如下：

- `categories`：数组，分类字段为 `id`、`name`、`parentId`、`sort`、`status`、`createTime`、`updateTime`。
- `hotProducts`：数组，商品字段为 `id`、`categoryId`、`categoryName`、`name`、`subtitle`、`mainImage`、`price`、`originalPrice`、`sales`、`status`、`stock`、`createTime`、`updateTime`。
- `recommendProducts`：数组，推荐商品字段为 `recommendId`、`recommendTitle`、`sort`、`productId`、`productName`、`subtitle`、`mainImage`、`price`、`originalPrice`、`sales`、`status`、`stock`、`createTime`、`updateTime`。
- 结论：`recommendProducts` 不返回 `id/name`，而是返回 `productId/productName`，前端必须通过统一 `normalizeProduct` 兼容该结构，否则会出现商品 ID 为 0 或“未命名商品”。

`GET /api/product/page?pageNum=1&pageSize=10` 返回 `code=200`，分页结构为 `total/pages/pageNum/pageSize/records`。`records` 商品字段为 `id`、`categoryId`、`categoryName`、`name`、`subtitle`、`mainImage`、`detail`、`price`、`originalPrice`、`sales`、`status`、`stock`、`lockedStock`、`createTime`、`updateTime`。

`GET /api/product/{id}` 返回 `code=200`。商品详情字段与商品分页基本一致，包含 `mainImage`。本次复查中，商品详情的 `mainImage` 已有 MinIO URL，而首页热门缓存中同一商品仍可能是空图，说明首页缓存和热门商品缓存需要清理后重新从商品服务读取最新字段。

`GET /api/coupon/page?pageNum=1&pageSize=10` 返回 `code=200`。优惠券分页字段为 `id`、`name`、`amount`、`minAmount`、`totalStock`、`availableStock`、`startTime`、`endTime`、`status`、`createTime`、`updateTime`。

`GET /api/coupon/my` 携带普通用户 token 返回 `code=200`。当前测试用户未领取优惠券时 `data=[]`；领取后应返回 `userCouponId`、`couponId`、`couponName`、`amount`、`minAmount`、`status`、`receiveTime`、`useTime`、`orderId`、`startTime`、`endTime`。

`GET /api/order/admin/page?pageNum=1&pageSize=10`：

- 当前 9050 运行实例经 Gateway 返回 `code=500`、`message=系统异常`。
- 使用当前源码已构建 jar 临时启动到 `19050`，直接请求 `/admin/page?pageNum=1&pageSize=10` 并传 `X-User-Id=39`、`X-User-Role=ADMIN` 返回 `code=200`。
- 管理员订单分页字段为 `id`、`orderNo`、`userId`、`totalAmount`、`discountAmount`、`payAmount`、`status`、`receiverName`、`receiverPhone`、`receiverAddress`、`payTime`、`deliveryCompany`、`trackingNo`、`deliveryTime`、`receiveTime`、`createTime`。
- 使用当前构建 jar 临时启动到 `19050` 后，调用 `POST /{orderId}/ship` 并传 `deliveryCompany`、`trackingNo` 返回 `code=200`，订单状态从 `1` 变为 `2`。
- 结论：后端源码和已构建 jar 中管理员订单分页接口可用；当前 9050 需要用最新构建重启 order-service 后再通过 Gateway 验证。

### 本阶段修复依据

- 首页热门商品按 `id/name/mainImage` 适配。
- 首页推荐商品按 `productId/productName/mainImage` 适配。
- 商品详情、商品分页、购物车、订单确认统一走商品字段归一化或等价字段适配。
- 首页真实图片替换后需要清理 `youxuan:home:index`、`youxuan:home:hot-products`、`youxuan:home:recommend-products` 和相关 `youxuan:product:detail:*`。
